---
import { joinClasses } from "../utils";

interface Props {
  style?: string;
}

const { style } = Astro.props;
let classes = "mb-5 lg:mb-8 text-4xl font-semibold";

if (style) {
  classes = joinClasses(classes, style);
}
---

<h1 class={classes}>
  <slot />
</h1>
